{
  "$id": "model.materialized.schema.json",
  "title": "Schema Model Materialized",
  "description": "DEPRECATED: use `materialization` instead. `materialization` accepts the same shorthand strings (\"incremental\" | \"ephemeral\") plus the full object form for `format` / `partitions` / `strategy` configuration. Replace `\"materialized\": \"incremental\"` with `\"materialization\": \"incremental\"`.",
  "deprecated": true,
  "type": "string",
  "enum": ["ephemeral", "incremental"]
}
